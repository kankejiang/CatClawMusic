# 猫爪音乐 v1.8.5 更新日志

> 自 v1.8.4 以来共 60 个提交，涵盖 AI 歌单、歌词解析、播放页/桌面歌词、统计性能、Android 交互与打包修复等。

## ✨ 新增功能

- **AI 歌单体验升级**
  - 支持流式思考过程实时显示（AI 生成中可见“正在思考”内容）
  - AI 歌单改为串行逐张生成，生成一张立即上屏，避免一次性等待全部完成
  - 后续歌单会参考前面已生成的歌单名，减少主题重复
  - 候选池扩大到 120 首，并告知 AI 已使用歌曲序号，降低 5 张歌单之间的歌曲重复
  - 新增“重新生成”入口：移动端 AI 歌单标题栏右侧、桌面端“AI 生成”按钮可点击
  - AI 歌单/每日推荐候选池跟随音乐库“发现页来源”过滤（本地/网络/混合）
  - 切换发现页来源后自动清空 AI 歌单缓存并按新来源重新生成

- **歌词系统大幅增强**
  - 网易云歌词宿主直连（eapi 加密接口），一次获取原文/译文/罗马音三流
  - 支持 KRC（酷狗）/ QRC（QQ）本地歌词解密解析
  - 歌词设置新增“显示译文 / 显示罗马音”开关
  - 支持 lx-music 扩展歌词四流机制（原文/译文/罗马音）
  - 本地歌词匹配改为两步精确匹配（歌名搜索 + 歌手验证）
  - 在线歌词缓存升级为三行结构，离线可用

- **AI 供应商新增 OpenCode Go**
  - OpenAI 兼容网关，内置 13 个模型，默认 `deepseek-v4-flash`
  - 预设对齐官方 `/v1/chat/completions` 兼容模型，移除不存在的 free 模型

- **发现页**
  - 发现页子 Tab“报告”改名为“统计”
  - 安卓/手机端发现页 Hero 网格支持插件快捷入口卡

## 🚀 性能优化

- **统计页大幅提速**
  - 新增 `DailyPlayStat / HourlyPlayStat / DailySongStat` 汇总表
  - 播放会话写入时增量更新汇总，统计页改为读取汇总小表，不再全量拉取 `PlaySession`
  - 旧版本首次打开统计页时自动从历史会话重建汇总
  - 统计结果按时间范围缓存，切换“近 7 天 / 近 30 天 / 全部”秒开
  - 移除启动时统计预热，避免给播放页/启动造成压力

- **AI 歌单生成更快更省**
  - 推理力度固定 low，降低 token 消耗
  - 5 张歌单拆分并行生成（并发 2 防限流），失败隔离不影响其他歌单
  - AI 歌单封面后台解析完成后自动刷新卡片

## 🖥️ Windows / 桌面歌词修复

- 当前行放大改绘制层方案（Win2D Transform），修复英文歌词滚动不跟随
- 修复当前行放大后字体下半部溢出
- 修复长歌词换行后逐字着色第二行提前亮
- 桌面端刷新按钮对齐移动端，支持转圈反馈
- 模型管理删除无反应修复

## 🤖 Android 交互优化

- 禁用 ViewPager2 触摸滑动切 Tab，只通过 TabBar 切换，根治卡片区域滑动误切 Tab
- 发现页手势禁用索引修正
- 新建/编辑模型按钮无反应修复
- 音乐库歌曲列表“⋮ 更多”按钮：Android 改为长按弹出菜单，Windows 改为单击右键
- 安卓竖屏 AI 歌单卡片缩小为与每日推荐同尺寸
- 安卓启动页改为可爱中英文文字（已去掉 Logo）

## 🔧 其他修复

- AI 候选池只从收藏挑歌问题：新增全库均匀采样并与个性化交替混合
- AI 歌单/推荐每张只有 1 首歌问题：候选列表改用连续序号代替数据库主键 ID
- 智谱 API 地址多拼 `/v1` 修复
- 模型管理删除、刷新按钮无反应等问题修复
- 修复 Android Release 构建时模型编辑页缺失点击事件的问题

## 📦 安装包

- Android APK：`CatClawMusic.Maui/bin/Release/net10.0-android/publish/com.catclaw.music-Signed.apk`
- Windows 安装包：`release/windows/catclaw.music-1.8.5-Setup.exe`
