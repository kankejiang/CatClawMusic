## 猫爪音乐 v1.8.4

### 🤖 AI 助手（本次重点）
- **联网能力全面升级**（照搬 opencode 方案）：Exa 托管 AI 搜索（结果更准）+ fetch_web_page 抓取正文；搜索三源并行 + 超时收紧，提速明显
- **工具调用管线重写**：参数校验 / 只读工具并行执行 / 智能结果截断（上限 10000 字节），多工具任务一次完成
- **对话流式输出**：思考过程与正文实时逐字显示，推理过程保持展开
- **浏览器接入**：browser_open 让 Yuki 直接控制内置浏览器读动态/反爬页面；预览改为聊天页顶部小窗口
- **download_file 工具**：Yuki 可直接帮用户下载文件（复用下载管理器）
- **音乐下载**：新增 netease_download（网易云免费直连，最高 320K）、kuwo_download（酷我免费直连**真无损 FLAC**）
- **音乐源配置化 + 自修复**：update_music_source 工具让 Yuki 能诊断并修复失效的音乐源接口
- 修复：AI 聊天 Android 崩溃（NetworkOnMainThreadException，请求统一切线程池）、服务端返回空 choices 时解析崩溃、工具结果单行 JSON 截断丢内容、reasoning_effort=auto 导致 DeepSeek 400
- 修复：模型管理页"切换备"误改主模型；备用模型未启用时不参与回退

### 🎧 播放修复
- **修复 Yuki 播放后进度条不走**：ExoPlayer 首次创建落在非主线程，setter 抛异常导致监听器未注册（强制主线程创建 + 双保险补启动进度定时器）
- 修复封面临时文件删除竞态导致的黑图（Glide ENOENT）；播放页封面显示前兜底

### 📱 Android
- 发现页卡片滑动不再误切 tab（ViewPager2 手势仲裁完整修复：嵌套滚动通道堵死 + 双保险挂载）
- 启动提速：三段串行改三路并行预加载；启动页等封面/歌词就绪再进主界面
- 本地扫描三来源（MediaStore/SAF/自定义文件夹）并行，1000 首导入不再卡死
- Debug 构建统一使用正式 keystore 签名（根治交替安装 JNI 冲突）

### 🎨 发现页（竖屏对齐横屏）
- Hero 卡改方形卡片横向单行；AI 入口卡并入首卡；新增 AI 歌单区块（每天 5 个主题歌单）
- 推荐专辑改两行横滑；卡片整体紧凑化，一屏显示更多内容

### 📥 下载
- **磁力（magnet/BT）下载支持**（内置 MonoTorrent 引擎）
- 下载管理：完成任务点击打开文件/目录；删除失败不再静默吞异常（弹窗提示原因）；Windows 嵌入模式按钮/弹窗修复

### 🧩 插件
- **插件更新检查与一键更新**；支持 Gitee 仓库安装与更新
- **通用插件快捷入口**（IQuickEntryPlugin）：插件可在发现页注册入口卡（如网易云私人漫游），支持排序

### 🖥 Windows
- 逐字歌词按对齐方式内部绘制、宽度钳制修复（居中/居右超出容器、最后一字裁剪、多字同时着色）
- 备份恢复按钮修复；备份目录移至文档；桌面首次加载修复

### 📋 其他
- 关于页版权年份动态读取系统时间；音乐源维护提示词去硬编码年份

### 📦 安装包
- Windows: catclaw.music-1.8.4-Setup.exe（Inno Setup 安装程序）
- Android: com.catclaw.music-1.8.4-Signed.apk
