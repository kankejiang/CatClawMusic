# 2026-06-15 工作日志

## 删除效果差的 API (00:38)

### 用户需求
"删除那些不好用的api"
→ 删除 MusicBrainz、TheAudioDB、iTunes、Wikipedia 四个对中文艺术家效果差的 API

### 实现内容

1. **删除文件**
   - `CatClawMusic.Data/MusicBrainzScraper.cs` - 已删除
   - `CatClawMusic.Data/TheAudioDbScraper.cs` - 已删除

2. **精简 `MultiSourcePhotoScraper.cs`**
   - 移除 iTunes 搜索方法（`SearchItunesAsync`）
   - 移除 Wikipedia 搜索方法（`SearchWikipediaAsync`、`SearchWikipediaFallbackAsync`）
   - 只保留 QQ 音乐搜索（华语覆盖最好）
   - `SourceName` 改为 "QQ音乐"

3. **更新 `MainApplication.cs`**
   - 移除 `MusicBrainzScraper` 和 `TheAudioDbScraper` 的 singleton 注册
   - 移除 `IArtistMetadataScraper` 中对应的注册
   - 保留：网易云 → 豆瓣 → QQ音乐

4. **更新 UI 布局**
   - `fragment_artist_match.xml`：只保留网易云、豆瓣、QQ音乐 三个 Chip
   - `fragment_artist_match_detail.xml`：同上

5. **更新 Fragment 映射**
   - `ArtistMatchFragment.cs`：`SourceChipToName` 只保留三个来源
   - `ArtistMatchDetailFragment.cs`：同上

### APK 构建
- 构建成功：`publish-apk/CatClawMusic.UI-Signed.apk` (44.5 MB)
- 版本：v1.4.8

### 剩余数据源
- **网易云**：中文艺术家覆盖好，有详情接口
- **豆瓣**：中文简介质量高（网页抓取，可能不稳定）
- **QQ音乐**：华语覆盖最好，已增强元数据获取（性别/地区/生日/简介）

---

## 添加百度百科刮削器和扩展艺术家元数据 (01:00)

### 用户需求
用户提供了百度百科截图，显示有丰富的艺术家信息（本名、昵称、民族、国籍、出生地、星座、身高、经纪公司、代表作品、职业等），要求：
1. 接入百度百科作为新的数据源
2. 扩展艺术家元数据字段，支持更多信息

### 实现内容

#### 1. 扩展 `ArtistSearchResult` 模型
文件：`CatClawMusic.Data/IArtistMetadataScraper.cs`

新增字段：
- `RealName` (本名)
- `Nickname` (昵称)
- `Ethnicity` (民族)
- `BirthPlace` (出生地)
- `Education` (毕业院校)
- `Zodiac` (星座)
- `Height` (身高)
- `Agency` (经纪公司)
- `RepresentativeWorks` (代表作品)
- `Occupation` (职业)

#### 2. 创建百度百科刮削器
文件：`CatClawMusic.Data/BaiduBaikeScraper.cs`

功能：
- 实现 `IArtistMetadataScraper` 接口
- 搜索艺术家：`SearchArtistsAsync()`
- 抓取详情页：`FetchArtistDetailAsync()`
- 解析基本信息框：`ParseInfobox()` - 解析 `<dt>`/`<dd>` 结构的 key-value 对
- 提取简介：`ExtractDescription()`
- 提取封面图片：`ExtractCoverImage()`
- SourceName：`"百度百科"`

#### 3. 更新依赖注入
文件：`CatClawMusic.UI/MainApplication.cs`

- 注册 `BaiduBaikeScraper` 到 DI 容器
- 更新 `IArtistMetadataScraper` 服务集合，百度百科排在第二位（网易云 → 百度百科 → 豆瓣 → QQ音乐）

#### 4. 更新元数据保存逻辑
文件：`CatClawMusic.UI/Helpers/ArtistMetadataSaver.cs`

- 重写 `ArtistMetadataDto` 类，新增 12 个字段
- 更新 `SaveMetadataAsync()` 方法，保存扩展信息到 JSON

#### 5. 更新 UI 布局
文件：`CatClawMusic.UI/Resources/layout/fragment_artist_detail.xml`

新增 TextView：
- `tv_artist_realname` (本名)
- `tv_artist_extra_info` (扩展信息，显示民族/出生地/星座/身高/经纪公司/代表作品/职业等)

#### 6. 更新艺术家详情页逻辑
文件：`CatClawMusic.UI/Fragments/ArtistDetailFragment.cs`

- 添加 `_artistRealname` 和 `_artistExtraInfo` 字段
- 在 `OnViewCreated` 中初始化新 TextView
- 在 `LoadOnlineInfoAsync` 中添加扩展信息显示逻辑
- 格式化显示：本名（如果与艺名不同）、民族、出生地、星座、身高、经纪公司、代表作品、职业

#### 7. 更新探索设置 UI
文件：
- `fragment_artist_match.xml`
- `fragment_artist_match_detail.xml`

- 添加"百科"Chip（在网易云后面）

#### 8. 更新 Fragment 映射
文件：
- `ArtistMatchFragment.cs`
- `ArtistMatchDetailFragment.cs`

- 更新 `SourceChipToName` 字典，添加 `["chip_baidubaike"] = "百科"`

### 修复的编译错误

1. **错误：`BaiduBaikeScraper` 不实现接口成员 `SourceName`**
   - 原因：之前用 `public const string SourceName = "百度百科";`（字段），但接口要求属性
   - 修复：改为 `public string SourceName => "百度百科";`（属性）

2. **错误：`WebUtility` 不存在**
   - 原因：`WebUtility.HtmlDecode` 需要 `System.Net` 命名空间
   - 修复：添加 `using System.Net;`

3. **错误：`BaikeEntry` 构造函数参数不匹配**
   - 原因：`BaikeEntry` 是 record，但代码中使用 `new BaikeEntry(string, string)` 时参数名称不匹配
   - 修复：重写 `BaiduBaikeScraper.cs`，使用正确的 record 语法 `private record BaikeEntry(string Title, string Url);`

4. **错误：`ExtraInfo` 是属性，不能用 `ref` 传递**
   - 原因：`ArtistSearchResult.ExtraInfo` 是属性，C# 不允许 `ref` 到属性
   - 修复：在 `ParseInfobox` 中使用局部变量 `var extraInfo = "";` 代替 `ref`

5. **错误：switch 语句包含多个相同标签（"艺名"和"星座"）**
   - 原因：代码中 `case "艺名"` 出现了两次（一次是"艺名"，一次是"艺名"的 Unicode normalization 问题）
   - 修复：重写 `ParseInfobox` 的 switch 语句，确保每个 label 只出现一次

6. **错误：缺少 `List<string>` 的 using 引用**
   - 原因：`ArtistDetailFragment.cs` 中使用了 `new List<string>()` 但没有 `System.Collections.Generic` 引用
   - 修复：添加 `using System.Collections.Generic;`

### APK 构建
- 构建成功：`publish-apk/CatClawMusic.UI-Signed.apk` (43 MB)
- 版本：v1.4.9
- 构建时间：1分24秒

### 当前数据源优先级
- **网易云**：中文艺术家覆盖好，有详情接口
- **百度百科**：丰富的艺术家元数据（本名、昵称、民族、出生地、星座、身高、经纪公司、代表作品、职业等）
- **豆瓣**：中文简介质量高（网页抓取，可能不稳定）
- **QQ音乐**：华语覆盖最好，已增强元数据获取（性别/地区/生日/简介）
