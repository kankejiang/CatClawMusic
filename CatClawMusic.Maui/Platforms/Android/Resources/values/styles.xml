<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- 全面屏主题：状态栏和导航栏透明，内容延伸到系统栏下方 -->
    <style name="MainTheme" parent="Maui.SplashTheme">
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowTranslucentNavigation">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <style name="MainTheme.Base" parent="MainTheme" />

    <!-- 启动画面主题：在 Maui.SplashTheme 基础上仅换底部品牌字样（图标沿用生成的 app_icon），
         并把 windowBackground 改为纯 #11141D——启动画面淡出瞬间若主界面首帧未就绪，
         露出的是纯深色底而不是旧版静态图标（避免"闪一下旧启动画面"） -->
    <style name="CatSplashTheme" parent="Maui.SplashTheme">
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
        <item name="android:windowSplashScreenBrandingImage">@drawable/splash_branding</item>
        <item name="android:windowBackground">#11141D</item>
    </style>
</resources>
